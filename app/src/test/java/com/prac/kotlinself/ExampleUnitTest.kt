package com.prac.kotlinself

import com.prac.kotlinself.repository.AssetLoader
import com.prac.kotlinself.repository.HomeAssetDataSource
import kotlinx.coroutines.withContext
import org.junit.Test

import org.junit.Assert.*
import org.junit.runner.RunWith
import kotlin.coroutines.coroutineContext

/**
 * Example local unit test, which will execute on the development machine (host).
 *
 * See [testing documentation](http://d.android.com/tools/testing).
 */

/*@RunWith(CenterFragment::class)*/
class ExampleUnitTest {
    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }

  /*  fun getJsonString(){
        withContext()
        context
        AssetLoader(context)
        val homeAssetDataSource = HomeAssetDataSource()
    }*/
}